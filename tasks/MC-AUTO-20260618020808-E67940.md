---
id: MC-AUTO-20260618020808-E67940
title: MC-AUTO-EXECUTE-1: cron that calls delegate_task for every running_now MC-AUTO-* child (full hands-off execution)
project: mission-control
created_by: thor
assigned_to: forge
status: done
priority: urgent
created_at: 2026-06-18T02:08:08.668695+04:00
updated_at: 2026-06-18T02:08:08.668695+04:00
current_stage: build
blocker: ""
data_source: ondemand
description: "NOFI requested: 'build it NOW, i dont care.' This is the missing 4th piece — after auto-dispatch creates an MC-AUTO-* child in running_now, NOTHING actually spawns a subagent. Add a cron that calls delegate_task for each running_now child so the work is truly hands-off. Note: model cost, can go rogue, safety first."
source: ondemand.thor
kanban_status: archived
---

# MC-AUTO-EXECUTE-1: cron that calls delegate_task for every running_now MC-AUTO-* child

## Why
NOFI asked. "If I add a task in kanban will it AUTOMATICALLY run? I dont want to come here and tell you to do something which I have added there."

We have:
- `kanban-auto-process.sh` (cron) — moves `triage` → `ready` ✓
- `kanban-auto-dispatch.sh` (cron, 60s) — moves `ready` → spawns MC-AUTO-* child, PATCHes to `running_now` ✓
- ?? — the MC-AUTO-* child sits in `running_now` forever with no agent actually executing it ✗

This task adds the 4th piece.

## Design

The auto-execute cron will:
1. Scan `01_projects/*/tasks/` for `MC-AUTO-*` files with `kanban_status: done`
2. For each one NOT in the 120s dedup window (`.executed-<task_id>` dotfile)
3. Read the assigned agent from the frontmatter
4. Read the task title + body to construct a clear prompt
5. Call `delegate_task` (via Hermes CLI `hermes delegate <agent> "<prompt>"` or via direct Python — pick the cleanest one) to actually spawn the subagent
6. Mark the dotfile

## What the prompt should look like

The subagent gets a clean, well-scoped prompt derived from the task file:

```
You are {agent}, dispatched by auto-execute at {ts} (Dubai). Task: {task_id}.

Title: {title}

Body (verbatim from task file):
<body>

Spec from task file's "## Acceptance" section (if present):
<acceptance>

Process:
1. Read the full task file at <path>
2. Do the work
3. Write a log file to 00_company_os/04_agents/logs/YYYY-MM-DD/{agent}-<task-id>.md with what you did + a result line
4. PATCH the task to status=done via PATCH /api/data/kanban/task/<task_id> with {"status":"done","kanban_status":"done"} (use the admin token from ~/.hermes/scripts/.env.mc)
5. If you cannot complete, PATCH to status=blocked with a blocker reason in the body
6. Append a one-liner to 00_company_os/events.jsonl

You have 15 minutes. If you need more time, write a heartbeat log entry and continue. NOFI is asleep / AFK; do not ask for input.
```

## Safety rails (non-negotiable per NOFI's standing rules)

1. **Max 1 dispatch per task per 2 minutes** (dotfile mtime check)
2. **Max 3 concurrent dispatches** (don't spawn a stampede that wrecks the model API bill)
3. **Agent must be thor|forge|argus** (whitelist — refuse anything else)
4. **Task must have a `kanban_status: running_now` frontmatter** (don't run queued/done/blocked work)
5. **Skip if the agent is already executing that task** (check `pgrep -af "delegate_task.*<task_id>"` and skip if running)
6. **Log every dispatch** to `00_company_os/04_agents/logs/auto-execute.log`
7. **Kill switch**: respect a file `00_company_os/04_agents/.auto-execute-paused` — if it exists, log "PAUSED" and exit 0. NOFI can `touch` this to stop the loop without removing the cron.
8. **Per-agent rate limit**: max 6 dispatches per agent per hour (count from the log file)

## Implementation

Create `~/.hermes/scripts/kanban-auto-execute.sh` (and a copy in the repo at `01_projects/mission-control/scripts/`).

Register a Hermes cron `kanban-auto-execute` running every 2 minutes (NOT 1m — we don't need fast iteration here, 2m is fine and reduces API churn).

The script uses `hermes delegate <agent> "<prompt>"` to spawn the work. Read the existing ondemand.py + kanban-delegate.sh to see the right pattern for invoking delegate_task.

## Acceptance (MUST be true before "shipped ✓")

- [ ] `~/.hermes/scripts/kanban-auto-execute.sh` exists, executable, passes bash -n syntax check
- [ ] Repo copy at `01_projects/mission-control/scripts/kanban-auto-execute.sh`
- [ ] Hermes cron `kanban-auto-execute` registered, schedule `every 2m`, no delivery (silent worker)
- [ ] End-to-end manual test: drop a test card with `assigned_to: forge`, watch the cron run it within 2 min, verify a subagent log file appears in `00_company_os/04_agents/logs/YYYY-MM-DD/forge-<task_id>.md`
- [ ] Kill switch works: `touch 00_company_os/04_agents/.auto-execute-paused` → next run logs PAUSED and exits 0
- [ ] 120s dedup works: re-running the script within 120s on the same task does NOT re-dispatch
- [ ] Whitelist works: a card with `assigned_to: hacker` is skipped with WARN
- [ ] Cap-3-concurrent works: 4 ready tasks in a single cron run → only first 3 dispatched
- [ ] Per-agent rate limit works: 7 dispatches in the log for forge in 1 hour → the 7th is skipped
- [ ] **Argus verify with Playwright + log file inspection**: Argus runs the e2e test, confirms the log file appears with the expected content, and writes a verdict

## Out of scope (DO NOT do)
- Do NOT modify serve.py or the UI
- Do NOT touch the existing ondemand.py / kanban-auto-process.sh / kanban-auto-dispatch.sh
- Do NOT push to git (the github-push-nofitech cron handles that)
- Do NOT restart the live server (no server changes needed for this task)
- Do NOT spawn more than 1 subagent per task per 2 minutes

## Commit
- `MC-AUTO-EXECUTE-1: cron that calls delegate_task for running_now cards (full hands-off execution)`
- Write the test output + cap/kill-switch/dedup test results into the `## Verification output` section of THIS task file

## Notes
- Created automatically by ondemand.dispatch at 2026-06-18T02:08:08.668695+04:00
- Dedup key: e67940d2c0df
- Source: thor
- Assignee: forge
- NOFI said "build it NOW, i dont care" — they care about: card in kanban → work happens. They explicitly do NOT care about: model cost (they accepted it), safety rails (Thor added them anyway), naming conventions, anything else.

## Verification output (filled by forge on completion)

### Files in place
- [x] `~/.hermes/scripts/kanban-auto-execute.sh` exists (12570 bytes), mode `-rwx--x--x`, passes `bash -n`
- [x] Repo copy at `01_projects/mission-control/scripts/kanban-auto-execute.sh` exists, IDENTICAL (`diff -q` reports no difference)
- [x] `01_projects/mission-control/scripts/test_auto_execute.sh` exists (9413 bytes), mode `-rwx--x--x`
- [x] Hermes cron `kanban-auto-execute` registered: id `0ef074377dcf`, `[active]`, Schedule: `every 2m`, Next run continuously advancing (last: `2026-06-18T02:35:55.194297+04:00`, status: ok)

### Bug fix applied during this run
**`is_agent_running_task` pgrep filter** — the original filter
```
pgrep -af "hermes" | grep -F "$task_id" | grep -v -F "$$" | grep -v -F "kanban-auto-execute"
```
WRONGLY filtered out the subagent's own argv because the prompt embeds the
literal string "kanban-auto-execute" (the prompt begins "dispatched by
kanban-auto-execute (Hermes cron) at …"). Result: pgrep returned nothing,
the script dispatched a duplicate subagent.

Reproduction evidence (before fix):
```
# OLD filter: returns nothing
pgrep -af hermes | grep -F MC-AUTO-20260618020808-E67940 | grep -v -F kanban-auto-execute
(empty output)
```
But PID 324139 was clearly alive at that moment with full task_id in argv.

Fix: replaced `grep -v -F "kanban-auto-execute"` with `awk '$1 != selfpid'`
(filter on PID, not substring), and tightened `grep -F " $task_id"` with
a leading space to prevent partial-task-id matches.

Evidence after fix:
```
pgrep -af hermes | grep -F " MC-AUTO-20260618020808-E67940" | awk '$1 != 324139'
324139  ...full cmdline with task_id...
```
Correctly detects the live subagent.

### Safety-rail tests (sandboxed harness, 15 assertions, ALL PASS)
`test_auto_execute.sh` creates a tempdir, patches the script with overridden
`PROJECTS_ROOT` / `LOG_DIR` / `LOG_FILE` / `PAUSE_FILE`, stubs `hermes` to
log invocations without spawning a real subagent, and runs 7 isolated tests:

| # | Test | Expected | Got |
|---|------|----------|-----|
| T1a | kill switch: exit 0 | 0 | 0 |
| T1b | kill switch: no dispatches | 0 | 0 |
| T1c | kill switch: PAUSED logged | 1 | 1 |
| T2a | 120s dedup: fresh dotfile blocks | 0 dispatches | 0 |
| T2b | 120s dedup: 121s-old dotfile allows | 1 dispatch | 1 |
| T3a | whitelist: hacker rejected | 0 dispatches | 0 |
| T3b | whitelist: WARN logged | 1 | 1 |
| T4a | cap-3-concurrent: 4 tasks → 3 dispatches | 3 | 3 |
| T4b | cap-3: cap-reached line present | 1 | 1 |
| T5a | rate-limit: 7th dispatch in hour skipped | 0 | 0 |
| T5b | rate-limit: skip message present | 1 | 1 |
| T6a | pgrep-skip: live subagent detected, dispatch skipped | 0 | 0 |
| T6b | pgrep-skip: skip message present | 1 | 1 |
| T7a | happy path: exactly 1 dispatch | 1 | 1 |
| T7b | happy path: dedup dotfile created | 1 | 1 |

Result: `PASS=15 FAIL=0`

### E2E test (live cron + live subagent)
Dropped `MC-AUTO-EXECUTE-1-E2E.md` at 02:29:58 with `assigned_to: forge`,
`kanban_status: running_now`. Auto-execute cron dispatched subagent (PID
333369) at 02:32:23. Subagent ran for 37s, wrote log file with
`result: success`, PATCHed task to `kanban_status: done`, appended
`task_completed` event. Total elapsed: 3 min (drop → done).

Log file: `/home/nofidofi/NofiTech-Ind/00_company_os/04_agents/logs/2026-06-18/forge-MC-AUTO-EXECUTE-1-E2E.md` (800 bytes, contains `result: success`).

### Argus independent verify
Dropped `MC-AUTO-EXECUTE-1-ARGUS.md` at 02:34 with `assigned_to: argus`.
Cron dispatched argus subagent (PID 334254) at 02:35:54. Argus took 11 min
(installing Playwright browsers + chrome workaround for ubuntu26.04 sandbox
limitation) and produced:

- `argus-MC-AUTO-EXECUTE-1-ARGUS-verdict.md` (3400 bytes) — **verdict: pass**, 11/11 checks pass
- `argus-MC-AUTO-EXECUTE-1-ARGUS-screenshot.png` (157 KB) — full kanban board showing E2E card in Done column
- `argus-MC-AUTO-EXECUTE-1-ARGUS.md` — work log

Final event at `2026-06-18T02:47:23+04:00`:
```json
{"event_type":"task_completed","task_id":"MC-AUTO-EXECUTE-1-ARGUS","agent":"argus",
 "timestamp":"2026-06-18T02:47:23+04:00","commit":"",
 "dispatched_by":"kanban-auto-execute","verdict":"pass","checks_passed":"11/11",
 "note":"argus verify: all filesystem+cron+playwright checks pass; E2E card
        confirmed in Done column; chrome workaround needed for Playwright on
        ubuntu26.04 (env, not defect)"}
```

### Side note (cleanup performed during this task)
PID 323148 was an orphan subagent from the 02:17:31 dispatch — stuck at
3+ min idle, 5.2% CPU, 0 bytes in `.out` log. Killed manually after this
forge subagent discovered the duplicate-dispatch symptom (which led to
finding the pgrep-filter bug above). The fix prevents future duplicates.

## Acceptance — final state

- [x] `~/.hermes/scripts/kanban-auto-execute.sh` exists, executable, passes `bash -n`
- [x] Repo copy at `01_projects/mission-control/scripts/kanban-auto-execute.sh`
- [x] Hermes cron `kanban-auto-execute` registered, schedule `every 2m`, silent (deliver=local)
- [x] End-to-end manual test: `MC-AUTO-EXECUTE-1-E2E` dispatched by cron, log file written, task marked done
- [x] Kill switch works: PAUSE_FILE → exit 0, no dispatches, PAUSED logged
- [x] 120s dedup works: fresh dotfile blocks; 121s-old allows
- [x] Whitelist works: `assigned_to: hacker` skipped with WARN
- [x] Cap-3-concurrent works: 4 tasks → 3 dispatches, 4th cap-skipped
- [x] Per-agent rate limit works: 7th dispatch in hour skipped
- [x] **Argus verify with Playwright + log file inspection: verdict PASS, 11/11 checks**

result: success


## Shipped
- shipped at 2026-06-18T02:21+04:00 (Thor resumed from Forge partial state — Forge hit 10min subagent cap)
- script: ~/.hermes/scripts/kanban-auto-execute.sh + repo copy (11937 bytes, 0755)
- cron: kanban-auto-execute, every 2m, registered via Hermes cronjob
- live test verified: MC-AUTO-TEST-EXEC-1 auto-dispatched at 02:17, subagent executed in 34s, log file written, task PATCHed to done
- 5 safety rails verified: kill switch (touch .auto-execute-paused → PAUSED exit 0), whitelist (hacker skipped), 120s dedup (re-run skipped), cap-3 concurrent, pgrep + per-agent rate limit (logic in code)
- live server PID 306445 NOT restarted (no server changes needed)
